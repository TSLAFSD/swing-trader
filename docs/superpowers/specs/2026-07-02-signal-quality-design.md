# 시그널 품질 개선 설계 (2026-07-02)

## 배경

- 현재 활성 전략은 `breakout`(60일 신고가 돌파) 단 하나 → 알림이 항상 고점 부근 종목
  ("과매수"로 체감). 버그가 아니라 유일 활성 전략의 구조적 성격.
- 목표: (1) 진입 성격이 반대인 과매도/눌림목 전략을 검증 후 추가, (2) breakout의
  과열 진입을 데이터 근거로 걸러 승률 개선 시도, (3) 전반 결함 점검.
- 대원칙: 백테스트는 과거 성과이며 미래 수익을 보장하지 않는다. 생존 편향 고지 유지.
  검증 탈락은 정직하게 비활성 유지("개선 없음"도 결과다).

## Part C — 프로그램 전반 결함 점검 (선행)

2026-07-02 KR 스캔 크래시(0가격 봉 → ZeroDivisionError, 8a73dce로 수정)와 동류의
잠재 결함을 스캔한다:

- 0으로 나누기 / 빈 프레임 / NaN 전파 방어 누락 (fetcher → store → indicators →
  report 데이터 경계 중심)
- 발견 건은 심각도 분류 후 TDD(실패 테스트 → 수정 → 전체 스위트)로 해결

## Part A — 과매도/눌림목 전략 3종 Phase-4 검증

대상: `pullback`, `connors_rsi2`, `zscore_meanrev` (전부 현재 비활성).

1. 기존 하네스 `main.py backtest` 풀 검증 (US 80종목 게이팅 / KR 20종목 참고,
   IS/OoS·walk-forward·MC 1,000회·민감도 ±20%·레짐 슬라이스·벤치마크)
2. GateReport 판독 → **통과 전략만** `strategies.yaml` `enabled: true` + 검증
   성적 주석 (breakout 스타일)
3. 경계선·탈락 → 비활성 유지, 사유 보고
4. 활성 상한 7개 확인 (현재 1 → 최대 4)

## Part B — breakout 과열 가드 (사전등록 기준 채택)

1. breakout에 **선택적** 가드 파라미터 3종 구현 — YAML 미설정 시 기존과 동일
   동작(베이스라인 보존 불변식):
   - `max_ext_atr`: (종가−SMA20)/ATR ≤ k — 변동성 보정 이격도
   - `max_ext_pct`: 종가/SMA20 − 1 ≤ X%
   - `rsi_max`: RSI(14) ≤ 상한
2. 베이스라인 vs 가드 소규모 그리드(유형 3 × 값 2~3 — 과적합 방지) IS/OoS 비교
3. **채택 기준 (사전등록, 변경 금지)**: OoS 승률 ≥ 베이스라인 +3%p
   **AND** OoS PF ≥ 베이스라인 × 0.9 **AND** OoS 거래수 ≥ 베이스라인 × 0.7.
   미달 시 가드 미채택.
4. 채택 시 breakout은 새 파라미터로 Phase-4 게이트 **재통과 후에만** YAML 반영.

## 실행 순서

C(빠른 안전점검) → A(풀 검증, 소요 큼) → B(가드 구현·비교). B는 A와 검증 하네스를
공유하므로 이 순서가 실행 횟수 최소.

## 테스트 전략

- Part B 가드: 파라미터 부재 시 베이스라인 패리티 테스트 + 가드 발동 단위 테스트
- 모든 변경 전 `.venv/bin/pytest tests/ -q` 그린 유지
- YAML 변경은 검증 리포트 실행 출력 원문 첨부 후에만 (reporting-integrity)

---

## 실행 결과 (2026-07-02, 판정 확정)

### Part C — 완료 (커밋 8a73dce, 00af0e1, 957f413)

1. KR 스캔 크래시: 0가격 봉 ZeroDivisionError — 3중 방어로 수정
2. 시그널 루프 종목별 가드 + vol_ma20=0 방어
3. **서킷브레이커 무력화 버그**: trailing_stats가 tail(n)을 실현 필터보다 먼저
   실행 → 실현 표본 항상 0 → 브레이커 출시 후 미작동. 수정 후 실데이터 검증:
   breakout trailing-20 = 평균 -16.9% / 승률 20% / PF 0.13 → 중단 조건 충족
   (단일 전략 안전장치로 실제로는 유지 + 경고 발송).

### Part A — 3종 전부 게이트 탈락, 비활성 유지 (정직 보고)

검증 실행 출력 원문 (US 77종목 게이팅 / KR 20종목 참고, 생존 편향 고지):

| 전략 | OoS PF | OoS WR | OoS n | 탈락 게이트 | 판정 |
|---|---|---|---|---|---|
| pullback | 0.69 | 39.1% | 23 | G1·G2·G3·G5 | FAIL |
| connors_rsi2 | 1.07 | 62.3% | 470 | G5 (rsi2_entry ±20% → PF 0.64/0.74) | FAIL(경계선) |
| zscore_meanrev | 1.77 | 57.1% | 14 | G6 (n=14<20) | FAIL(표본만) |
| (참고) breakout 재검증 | 1.03 | 38.7% | 93 | — 전체 통과 | PASS 유지 |
| (참고) squeeze | 0.43 | 41.2% | 17 | G1·G3·G5·G6 | FAIL |
| (참고) wyckoff_spring | 0.73 | 41.7% | 12 | G1·G3·G5·G6 | FAIL |

- breakout은 재통과했으나 2026-06 급락 구간이 OoS에 포함되며 PF 2.55→1.03으로
  약화. 활성 유지하되 서킷브레이커(수정됨)가 주간 감시.
- zscore_meanrev는 표본만 부족 (bear PF 4.47) — 표본 축적 시 최우선 재검증.

### Part B — 사전등록 기준 미달, 가드 미채택 (정직 보고)

기준: OoS WR ≥ 베이스라인 +3%p AND PF ≥ ×0.9 AND n ≥ ×0.7 (변경 없이 적용)

```
variant                 OoS n   OoS WR   OoS PF    IS PF  verdict
baseline                   93    38.7%     1.03     1.85  —
max_ext_atr=2               9    22.2%     0.85     1.69  ❌
max_ext_atr=3              46    30.4%     0.96     1.35  ❌
max_ext_atr=4              82    37.8%     1.07     1.94  ❌
max_ext_pct=10             55    38.2%     1.08     1.30  ❌
max_ext_pct=15             73    38.4%     1.13     1.28  ❌
max_ext_pct=20             87    37.9%     1.11     1.21  ❌
rsi_max=70                 24    33.3%     1.33     2.10  ❌
rsi_max=75                 67    37.3%     1.12     1.84  ❌
rsi_max=80                 88    37.5%     1.01     1.80  ❌
```

- 어떤 변형도 승률 조건(≥41.7%)을 충족하지 못함 → 미채택.
- 참고(소표본 주의): 2026-06 실현 43건 소급 적용에서는 max_ext_pct=15가
  통과군 +2.2% vs 차단군 -13.9%로 급락 구간 방어력을 보임. US 3년 표본에서는
  PF만 소폭 개선(1.03→1.13), 승률 개선 없음. 채택하려면 소유자 승인으로
  기준 변경 + Phase-4 재통과 필요 (선택지로만 기록).
- 가드 파라미터 3종은 코드에 구현되어 있으며 YAML 미설정 시 베이스라인과
  동일 동작 (테스트로 패리티 보장).

### 후속 권고 (미실행 — 소유자 결정 필요)

1. **(전략,시장) 단위 서킷브레이커** — 현재 전략 단위 통합이라 US가 KR 손실을
   희석. KR 실현 -16% 상황에서 KR만 중단하는 기능이 없음.
2. 단일 전략 안전장치 정책 재검토 — 유일 활성 전략이 중단 조건을 충족해도
   발송이 계속됨 (현재는 설계 의도).
3. zscore_meanrev 표본 축적 후 재검증 (OoS 20건 도달 시).
